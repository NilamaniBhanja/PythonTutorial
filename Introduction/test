<connectionStrings>
  <add name="RedshiftConnection"
       connectionString="Host=your-redshift-cluster.xxxxxx.us-east-1.redshift.amazonaws.com;
                         Port=5439;
                         Database=dev;
                         Username=awsuser;
                         Password=YourPassword;
                         SSL Mode=Require;"
       providerName="Npgsql" />
</connectionStrings>

using Npgsql;
using System.Collections.Generic;
using System.Configuration;

public class RedshiftRepository
{
    private readonly string _connectionString =
        ConfigurationManager.ConnectionStrings["RedshiftConnection"].ConnectionString;

    public List<Employee> GetEmployees()
    {
        var employees = new List<Employee>();

        using (var conn = new NpgsqlConnection(_connectionString))
        {
            conn.Open();

            string sql = "SELECT id, name FROM employee";

            using (var cmd = new NpgsqlCommand(sql, conn))
            using (var reader = cmd.ExecuteReader())
            {
                while (reader.Read())
                {
                    employees.Add(new Employee
                    {
                        Id = reader.GetInt32(0),
                        Name = reader.GetString(1)
                    });
                }
            }
        }

        return employees;
    }
}



Compilation Error
Description: An error occurred during the compilation of a resource required to service this request. Please review the following specific error details and modify your source code appropriately.

Compiler Error Message: CS0012: The type 'System.Object' is defined in an assembly that is not referenced. You must add a reference to assembly 'netstandard, Version=2.0.0.0, Culture=neutral, PublicKeyToken=cc7b13ffcd2ddd51'.

Source Error:


Line 16:                     <span class="icon-bar"></span>
Line 17:                 </button>
Line 18:                 @Html.ActionLink("Application name", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
Line 19:             </div>
Line 20:             <div class="navbar-collapse collapse">
