 private string GetXMLString(string Path)
 {
     StreamReader fs = File.OpenText(Path);
     String strPolicy = fs.ReadToEnd();
     return strPolicy;
 }

'controller.file(byte[], string)' is a method, which is not valid in the given context
