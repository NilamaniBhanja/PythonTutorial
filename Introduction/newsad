 if (PremiumObj.IsFullQuote)
 {
     //Checking start MASTPSourceNames for KYCStageStep 5Jan2022
     string KYCStageStep = string.Empty;
     if (PremiumObj.BusinessType.ToUpper() == "RENEWAL")
     {
         KYCStageStep = entities.MASTPSourceNames.Where(c => c.Name == PremiumObj.TPSourceName).Select(a => a.KYCRenewalStageStep).FirstOrDefault().ToString();
     }
     else
     {
         KYCStageStep = entities.MASTPSourceNames.Where(c => c.Name == PremiumObj.TPSourceName).Select(a => a.KYCStageStep).FirstOrDefault().ToString();
     }
     //Checking end MASTPSourceNames for KYCStageStep 5Jan2022
     #region Only TPSourceName KYC Validation allowed
     if (Convert.ToString(KYCStageStep) != "4")
     {
         #region For Normal Aggregator
         //Start Normal Aggregator FullQuote request (30Dec2022)
         if ((string.IsNullOrEmpty(PremiumObj.KYCStage) || Convert.ToString(PremiumObj.KYCStage) == "0"))
         {
             if (string.IsNullOrEmpty(PremiumObj.Aggregator_KYC_Req_No))
             {
                 strError = "Please enter Aggregator KYC Request Number.";
                 AuditLogObj.End();
                 AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                 return strError;
             }
             if (string.IsNullOrEmpty(PremiumObj.IC_KYC_No))
             {
                 strError = "Please enter IC KYC Number.";
                 AuditLogObj.End();
                 AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                 return strError;
             }
             //Start Checking from Middle Ware (30Dec2022)
             KYCValidationDAL kYCValidation = new KYCValidationDAL();
             if (!kYCValidation.CKYCValidationMiddelWare(PremiumObj.Aggregator_KYC_Req_No, PremiumObj.IC_KYC_No))
             {
                 strError = "Please enter the valid KYC details.";
                 AuditLogObj.End();
                 AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                 return strError;
             }
             //Start Checking from Middle Ware (30Dec2022)
         }
         //End Normal Aggregator FullQuote request (30Dec2022)
         #endregion
         else
         {
             if ((Convert.ToString(PremiumObj.KYCStage) == "1" || Convert.ToString(PremiumObj.KYCStage) == "2" || Convert.ToString(PremiumObj.KYCStage) == "3"))
             {

                 //var KYCStageStep = entities.MASTPSourceNames.Where(c => c.Name == PremiumObj.TPSourceName).Select(a => a.KYCStageStep).FirstOrDefault();

                 #region PolicyBazaar FullQuote request KYCStage 2
                 //Start PolicyBazaar FullQuote request (30Dec2022)
                 if (Convert.ToString(KYCStageStep) == Convert.ToString(PremiumObj.KYCStage) && Convert.ToString(PremiumObj.KYCStage) == "2")
                 {
                     if (string.IsNullOrEmpty(PremiumObj.Aggregator_KYC_Req_No))
                     {
                         strError = "Please enter Aggregator KYC Request Number.";
                         AuditLogObj.End();
                         AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                         return strError;
                     }
                     if (string.IsNullOrEmpty(PremiumObj.IC_KYC_No))
                     {
                         strError = "Please enter IC KYC Number.";
                         AuditLogObj.End();
                         AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                         return strError;
                     }
                     var result = entities.PolicyDetails.Where(a => a.CoverNoteNumber == PremiumObj.QuickQuoteNumber && a.PolicyNumber != null && a.PolicyNumber != "NA").Select(a => a.ID).Take(1);
                     if (result.Any())
                     {
                         strError = "The policy already been generated.";
                         AuditLogObj.End();
                         AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                         return strError;
                     }
                     KYCValidationDAL kYCValidation = new KYCValidationDAL();
                     if (!kYCValidation.CKYCValidationMiddelWare(PremiumObj.Aggregator_KYC_Req_No, PremiumObj.IC_KYC_No))
                     {
                         strError = "Please enter the valid KYC details.";
                         AuditLogObj.End();
                         AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                         return strError;
                     }

                 }
                 else if (Convert.ToString(KYCStageStep) == "2" && Convert.ToString(PremiumObj.KYCStage) != "1")
                 {
                     strError = "Please enter the correct KYC stage sequence.";
                     AuditLogObj.End();
                     AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                     return strError;
                 }
                 //End PolicyBazaar FullQuote request (30Dec2022)
                 #endregion

                 #region Coverfox,Policybazar b2b FullQuote request KYCStage 3
                 //Start Coverfox,Policybazar b2b FullQuote request (30Dec2022)
                 if (Convert.ToString(KYCStageStep) == Convert.ToString(PremiumObj.KYCStage) && Convert.ToString(PremiumObj.KYCStage) == "3")
                 {
                     if (string.IsNullOrEmpty(PremiumObj.CKYC_No))
                     {
                         strError = "Please enter CKYC Number.";
                         AuditLogObj.End();
                         AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                         return strError;
                     }

                 }
                 else if (Convert.ToString(KYCStageStep) == "3")
                 {
                     strError = "Please enter the correct KYC stage sequence.";
                     AuditLogObj.End();
                     AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                     return strError;
                 }
                 //End Coverfox,Policybazar b2b FullQuote request (30Dec2022)
                 #endregion

                 #region For KYCStage 1
                 //Start Policybazar first time FullQuote request (30Dec2022)
                 //if (Convert.ToString(KYCStageStep) != Convert.ToString(PremiumObj.KYCStage) && Convert.ToString(PremiumObj.KYCStage) == "1")
                 //{

                 //}
                 //End Policybazar first time FullQuote request (30Dec2022)
                 #endregion
             }
             else
             {
                 strError = "Please enter the correct KYC stage sequence.";
                 AuditLogObj.End();
                 AuditLogObj.InsertTPServiceAuditLog(null, requestXml, PremiumObj.QuickQuoteNumber, PremiumObj.IsFullQuote, "ValidateServiceInput", null, PremiumObj.TPSourceName, null, strError, null, null, null, null, PremiumObj.IsNilDepOptedInPrevPolicy);
                 return strError;
             }
         }
     }
     #endregion
     
 }
               
